{
  "dependencies": {
    "face-api.js": "^0.22.2",
    "mongoose": "^6.5.1",
    "papaparse": "^5.3.2",
    "react-spring": "^9.5.5"
  },
  "scripts": {
    "clean": "rimraf build && rimraf node_modules"
  },
  "devDependencies": {
    "rimraf": "^4.1.1"
  }
}
